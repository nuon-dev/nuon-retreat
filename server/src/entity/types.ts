export const enum AttendType{
    full = 'full',
    half = 'half',
}

export const enum MoveType {
    Together,
    driveCar,
    rideCar,
    goAlone,
}

export enum PermissionType{
    superUser,
    admin,
    userList,
    resetPassword,
    carpooling,
    permisionManage,
    showRoomAssignment,
    roomManage,
    showGroupAssignment,
    groupManage,
}
